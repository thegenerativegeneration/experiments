# QLoRA / SFT fine-tuning configuration
# ─────────────────────────────────────────────────────────────────────────────

# ── Base model ────────────────────────────────────────────────────────────────
# Any HuggingFace model ID or local path.
# Good multilingual starting points:
#   mistralai/Mistral-7B-Instruct-v0.3
#   meta-llama/Meta-Llama-3.1-8B-Instruct
#   Qwen/Qwen2.5-7B-Instruct
#   google/gemma-2-9b-it
model_name_or_path: mistralai/Mistral-7B-Instruct-v0.3

# ── Data ──────────────────────────────────────────────────────────────────────
train_file: data/train.jsonl
eval_file:  data/eval.jsonl
# Column that contains the list of chat messages.
messages_column: messages

# ── Output ────────────────────────────────────────────────────────────────────
output_dir: output/mhg-mistral-7b

# ── Quantisation (QLoRA) ──────────────────────────────────────────────────────
load_in_4bit: true
bnb_4bit_compute_dtype: bfloat16  # float16 if bfloat16 not supported
bnb_4bit_quant_type: nf4
bnb_4bit_use_double_quant: true

# ── LoRA adapter ──────────────────────────────────────────────────────────────
lora_r: 32
lora_alpha: 64
lora_dropout: 0.05
# Modules to attach LoRA adapters to.
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj
bias: none
task_type: CAUSAL_LM

# ── Training hyperparameters ──────────────────────────────────────────────────
num_train_epochs: 3
per_device_train_batch_size: 4
per_device_eval_batch_size: 4
gradient_accumulation_steps: 8   # effective batch = 32
learning_rate: 2.0e-4
lr_scheduler_type: cosine
warmup_ratio: 0.05
max_seq_length: 2048
packing: true                     # pack short examples to fill context window

# ── Optimisation ─────────────────────────────────────────────────────────────
optim: paged_adamw_32bit
fp16: false
bf16: true                        # set to false and fp16: true for older GPUs
gradient_checkpointing: true

# ── Checkpointing & logging ───────────────────────────────────────────────────
save_steps: 100
eval_steps: 100
logging_steps: 25
save_total_limit: 3
load_best_model_at_end: true
metric_for_best_model: eval_loss

# ── Reproducibility ───────────────────────────────────────────────────────────
seed: 42

# ── Push to Hub ───────────────────────────────────────────────────────────────
# Set push_to_hub: true and fill hub_model_id to publish after training.
push_to_hub: false
hub_model_id: ""          # e.g. "your-org/mhg-mistral-7b"
